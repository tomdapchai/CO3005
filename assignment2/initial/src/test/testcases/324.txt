
        func main() {
            for idx, val := range arr {
                putInt(idx);
                putInt(val);
            }
            for _, val := range arr {
                putInt(val);
            }
        }
            
        