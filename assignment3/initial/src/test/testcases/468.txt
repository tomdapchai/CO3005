
        func main(){
            for _, value := range [5]float{1.0,2,3.0,4.0,5.0} {
                putInt(value);
            }
            var value string;
            var index int;
            arr := [5]int{1,2,3,4,5};
            for index, value := range arr {
                putInt(index);
                putString(value);
            }
        }
        